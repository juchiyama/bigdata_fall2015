from rdt.data.mongo.source import Source

def features(doc):
	ft 

if __name__ == "__main__":
	source = Source(host="localhost",port=27017,database="reddit_stream",collection="big_combined")
	for doc in soure.find({"subreddit" : "UkrainianConflict"}):